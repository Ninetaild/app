# Saving Game

A React + Vite savings game with optional per-user Google Sheets backup.

## Local development

```bash
npm install
npm run dev
```

## Google Sheets setup

Set `VITE_GOOGLE_CLIENT_ID` in your local `.env` file. Never put OAuth client secrets, API secrets, or personal credentials in the repository.
